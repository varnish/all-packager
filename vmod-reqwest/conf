NAME=vmod-reqwest
VERSION=0.0.12
URL="https://github.com/gquintard/vmod-reqwest"
DOWNLOAD_URL="https://github.com/gquintard/vmod-reqwest/archive/refs/tags/v0.0.12.tar.gz"

UNTAR_DIR="${NAME}-${VERSION}"

DESC="Dynamic backends and HTTP requests in VCL"

LONG_DESC=$( cat << EOF
Dynamic backends and HTTP requests in VCL
 
EOF
)
MAINTAINER="Varnish Software <opensource@varnish-software.com>"

CHANGELOG_DATE="Thu Jul 24 2014"
