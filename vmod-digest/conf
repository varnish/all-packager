NAME=vmod-digest
VERSION=1.0.3
URL="https://github.com/varnish/varnishlog-json"
DOWNLOAD_URL="https://github.com/varnish/lib${NAME}/archive/refs/tags/lib${NAME}-${VERSION}.tar.gz"

UNTAR_DIR="lib${NAME}-lib${NAME}-${VERSION}"

DESC="Varnish Module (vmod) for computing HMAC, message digests and working with base64."

LONG_DESC=$( cat << EOF
Varnish Module (vmod) for computing HMAC, message digests and working with base64.
 All HMAC- and hash-functionality is provided by libmhash, while base64 is implemented locally.
EOF
)
MAINTAINER="Kristian Lyngstøl <opensource@varnish-software.com>"

CHANGELOG_DATE="Wed Mar 16 2016"